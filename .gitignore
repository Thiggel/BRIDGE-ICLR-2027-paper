# LaTeX build products.  The PDF is tracked deliberately so the current build is
# readable without a TeX installation; everything else here is regenerated by
# latexmk on any machine and only ever produces conflicts.
*.aux
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
